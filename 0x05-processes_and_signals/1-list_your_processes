#!/usr/bin/env bash
# A Bash script that displays all running processes in a user-oriented, hierarchical format including those without a TTY
ps -eo user,pid,%cpu,%mem,vsz,rss,tty,stat,start,time,cmd --forest
