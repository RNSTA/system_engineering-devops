#!/usr/bin/env bash
# or use: #!/bin/bash

# Get the PID of the running 7-highlander process (script, not editor)
pid=$(ps -eo pid,comm,args | grep "[7]-highlander" | awk '{print $1}')

# Kill only if a PID was found
if [ -n "$pid" ]; then
  kill -9 "$pid"
fi
